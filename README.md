# Laravel Front-End Product Management

simple front-end Laravel project for product management.

## Features
- Product table with DataTables plugin
- Add product via modal form
- Front-end validation using SweetAlert2
- Responsive layout using Bootstrap 5

## Installation
1. Clone the repo
2. Run `composer install`
3. Start local server with `php artisan serve`
